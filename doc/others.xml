<!-- W  others.tex            FORMAT documentation  B. Eick and C.R.B. Wright / 10-31-11 -->
<Chapter Label="Other Applications">
<Heading>Other Applications</Heading>
Up to this point our screens, i.e., normal subgroup functions, have yielded local formation residual subgroups, but there is no requirement that they do so. Screens for which the selected normal subgroups can be arbitrary have applications beyond formation theory. Chapter V of <Cite Key="CH"/> contains an account of a generalized normalizer theory built from them, and Wright (<Cite Key="WA"/> and <Cite Key="WB"/>) uses them to construct internal versions of formations that are conceptually related to ordinary formations much as Fitting sets are related to Fitting classes.
<P/>
A major application of the generalized normalizers is to speed up computation of complements to normal factors (see&nbsp;<Cite Key="EW"/>).
Suppose that <M>G</M> is a finite solvable group with an elementary abelian
normal subgroup <M>A</M> for which there exists a normal subgroup <M>N</M> of <M>G</M>
containing <M>A</M> such that <M>N/A</M> is nilpotent and <M>[N,A] = A</M>. Then <M>A</M> has a complement in <M>G</M>, and all complements
are conjugate---indeed, they can be viewed as generalized
<M>\mathcal{F}</M>-normalizers. We will show the idea, which of course is most useful with very large groups, by using <C>FNormalizerWrtFormation</C> to find a complement to an
elementary abelian normal subgroup, in this case to <M>K</M> in <M>S_4</M> with <M>N =
A_4</M>.
<P/>
We need to define a formation <C>F</C> in &GAP; (not a real formation,
of course, just a local version) such that <C>ScreenOfFormation( F )(s4,p)</C> returns <M>A_4</M> for
every call. In order to call <C>FNormalizerWrtFormation</C> we must also set the property
<C>IsIntegrated</C> to <K>true</K>.
<Example><![CDATA[
gap> preform := rec( name := "ForComplement",
> fScreen := function( H, p )
> return Subgroup( H, GeneratorsOfGroup( H ){[2,3,4]});
> end);;
gap> form := Formation(preform);
formation of ForComplement groups
gap> SetIsIntegrated(form, true);
]]></Example>
Now we may use  <C>FNormalizerWrtFormation</C>  with <C>s4</C> to
get the complement, an <M>S_3</M>. (Recall that unless <C>form</C> already
thinks it's integrated, <C>FNormalizerWrtFormation</C> will automatically
integrate <C>form</C> before running its computations, which may not be
the desired behavior.)
<Example><![CDATA[
gap> comp := FNormalizerWrtFormation(s4, form); Size(comp);
Group([ f1, f2 ])
6
]]></Example>
A user who wanted to employ the <M>\mathcal{F}</M>-normalizer technique to compute very
many complements in this way would probably wish to create a new &GAP;
function by extracting portions of the code that computes
<M>\mathcal{F}</M>-systems.
</Chapter>
